name: Node.js Package

on:
  push:
    branches:
      - 'main'


jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v4
        with:
          node-version: 25
      - run: npm ci
      - run: npm test